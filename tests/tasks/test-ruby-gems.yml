---
# Test if Ruby Gems can actual be used

- name: get gem version
  command: gem -v
  register: ruby2_test_fact_gem_version
- name: assert gem version is displayed and is at least version 2
  assert:
    that:
      - "'2.' in ruby2_test_fact_gem_version.stdout"
